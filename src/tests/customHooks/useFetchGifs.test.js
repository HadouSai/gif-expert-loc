import "@testing-library/jest-dom";
import { shallow } from "enzyme"
import { useFetchGifs } from "../../hooks/useFetchGifs";

describe('Testing my custom hooks', () => {
    test('should return the initial state', () => {
        /* const { giffs, loading } = useFetchGifs('Kurumi');
        console.log(giffs, loading); */
        // no pude necesita una dependencia especial 
    })

})
